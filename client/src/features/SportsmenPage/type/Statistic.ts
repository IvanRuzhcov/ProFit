export type Statistic = {
  id: number;
  user_id_param: number;
  weight: string;
  createdAt: string;
};

export type ChartBar = {
  id: number
  user_id_ChartBar: number;
  time: string;
  createdAt: string
};